{
  "name": "api_vercel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp .env.local dist/ && cp -r api dist/ && cp -r sanity dist/ && cp constants.js dist/",
    "start:vercel": "vercel dev --listen 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {},
  "dependencies": {
    "@sanity/client": "^7.4.1",
    "@sanity/preview-url-secret": "^2.1.11",
    "dotenv": "^16.4.5"
  }
}
