{
  "name": "visual-editor",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "node start.js",
    "deploy:production": "cd proxy && npx vercel --prod && cd .. && cd vercel_api && npx vercel --prod && cd .. && cd expo && npx vercel --prod && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "5.8.3",
    "ts-node": "^10.9.2",
    "concurrently": "^8.2.2"
  }
}
